# 说话人分离 sidecar 的可复现运行依赖。模型权重不进入代码仓，由 ModelScope 缓存管理。
funasr==1.3.14
modelscope==1.37.1
soundfile==0.14.0
# 已在 macOS + Python 3.12 的 FunASR smoke 验证；缺少它们会导致 preload 运行失败。
torch==2.12.1
torchaudio==2.11.0
